<Page
	x:Class="Translit.Views.Pages.DatabaseView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Title="Database"
	d:DesignHeight="500"
	d:DesignWidth="500"
	Background="{DynamicResource MaterialDesignPaper}"
	FontFamily="{DynamicResource MaterialDesignFont}"
	Loaded="Page_OnLoaded"
	TextElement.FontSize="13"
	TextElement.FontWeight="Regular"
	TextElement.Foreground="{DynamicResource MaterialDesignBody}"
	TextOptions.TextFormattingMode="Ideal"
	TextOptions.TextRenderingMode="Auto"
	mc:Ignorable="d">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition />
			<RowDefinition />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<StackPanel
			Grid.Row="0"
			Margin="50,0"
			HorizontalAlignment="Center"
			VerticalAlignment="Center">
			<TextBlock
				x:Name="TextBlockDatabase"
				Margin="0,5"
				HorizontalAlignment="Center"
				FontSize="20"
				Foreground="#303030"
				TextWrapping="Wrap" />
			<TextBlock
				x:Name="TextBlockDatabaseSize"
				Margin="0,5"
				HorizontalAlignment="Center"
				FontSize="15"
				Foreground="#303030"
				TextWrapping="Wrap" />
			<TextBlock
				x:Name="TextBlockSymbolsCount"
				Margin="0,5"
				HorizontalAlignment="Center"
				FontSize="15"
				Foreground="#303030"
				TextWrapping="Wrap" />
			<TextBlock
				x:Name="TextBlockWordsCount"
				Margin="0,5"
				HorizontalAlignment="Center"
				FontSize="15"
				Foreground="#303030"
				TextWrapping="Wrap" />
			<TextBlock
				x:Name="TextBlockLastLastUpdate"
				Margin="0,5"
				HorizontalAlignment="Center"
				FontSize="15"
				Foreground="#303030"
				TextWrapping="Wrap" />
		</StackPanel>
		<Button
			x:Name="ButtonDatabaseUpdate"
			Grid.Row="1"
			Margin="2,0"
			HorizontalAlignment="Center"
			materialDesign:ButtonProgressAssist.IsIndeterminate="False"
			materialDesign:ButtonProgressAssist.IsIndicatorVisible="True"
			materialDesign:ButtonProgressAssist.Value="-1"
			Click="ButtonDatabaseUpdate_OnClick"
			Content="{DynamicResource ButtonUpdate}"
			Style="{StaticResource MaterialDesignRaisedButton}" />
		<StackPanel
			x:Name="StackPanelProgress"
			Grid.Row="2"
			Margin="50,0"
			VerticalAlignment="Center"
			Visibility="Hidden">
			<TextBlock
				x:Name="TextBlockSymbolInserting"
				Margin="0,5"
				HorizontalAlignment="Center"
				FontSize="15"
				TextWrapping="Wrap" />
			<ProgressBar
				Name="ProgressBarSymbolInserting"
				Margin="0,20"
				Maximum="100"
				Minimum="0"
				Value="0" />
			<TextBlock
				x:Name="TextBlockWordInserting"
				Margin="0,5"
				HorizontalAlignment="Center"
				FontSize="15"
				TextWrapping="Wrap" />
			<ProgressBar
				Name="ProgressBarWordInserting"
				Margin="0,20"
				Maximum="100"
				Minimum="0"
				Value="0" />
		</StackPanel>
	</Grid>
</Page>